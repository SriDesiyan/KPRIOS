{
  "name": "kpyrios-acpia-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend install --legacy-peer-deps && npm --prefix frontend run build",
    "test": "npm --prefix frontend test"
  }
}
